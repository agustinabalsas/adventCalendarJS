function checkJump(heights) {
    // recorro el array
    // corroborando que sea menor al próximo
    // cuando el valor siguiente es menor al actual
    // corroboro que los próximos valores sean siempre menores al próximo
   
   
    for (let i = 0; i < heights.length ; i ++){
      if (heights[i] > heights[i+1]){
        false;
      }else{
     if (heights[i] > heights[i+1]){
        for (let j = i; j < heights.length ; j ++){
    
        }
      }
      }
    }
   }